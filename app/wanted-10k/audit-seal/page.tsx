import type { Metadata } from "next";
import { SiteNav } from "../../components/SiteNav";
import { AuditSealLab } from "./AuditSealLab";

export const metadata: Metadata = { title: "Independent Audit Trust — WANTED-10K", description: "Verify the audit seal, pinned registry credential, auditor-key binding, authorization, validity, and revocation locally.", alternates: { canonical: "/wanted-10k/audit-seal" } };

export default function AuditSealPage(){return <main className="sealPage"><SiteNav/><section className="sealHero shell"><span className="eyebrow"><i className="liveDot"/> AUDIT TRUST · SEAL 0.2-V1 + CREDENTIAL 0.2-V2</span><h1>Trust the seal.<br/><em>Then trust the key.</em></h1><p>Profile 0.2-V1 proves the manifest was signed by its declared auditor key. Profile 0.2-V2 closes the self-declared-key gap with an issuer-signed credential, pinned registry root, target authorization, bounded status freshness, validity, and revocation checks.</p><div className="sealHeroLinks"><a className="primary" href="#verify">Verify an audit package <span>↓</span></a><a className="secondary" href="/wanted-10k/audit-seal.json">Seal contract</a><a className="secondary" href="/wanted-10k/auditor-credential.json">Credential contract</a><a className="secondary" href="/wanted-10k/auditor-credential.schema.json">Credential schema</a><a className="secondary" href="/wanted-10k/audit-manifest.template.json">Credentialed example</a></div><div className="sealProof"><div><b>2</b><span>ED25519 SIGNATURES</span></div><div><b>14</b><span>HARD VERIFICATION CHECKS</span></div><div><b>24H</b><span>MAX STATUS WINDOW</span></div><div><b>0</b><span>NETWORK UPLOADS</span></div></div></section><div id="verify"><AuditSealLab/></div><section className="sealMethod"><div className="shell"><div className="sectionHead wantedHead"><div><span className="kicker">02 / TWO-LAYER TRUST</span><h2>Verify the bytes.<br/><em>Then verify the signer.</em></h2></div><p>The audit seal covers every manifest claim except its own digest and signature. Inside that seal, a separately issuer-signed credential binds the auditor and organization to the exact audit key, targets, and a bounded-fresh registry status proof.</p></div><div className="sealMethodGrid"><article><span>01</span><b>VERIFY SEAL</b><p>Reconstruct RFC 8785 bytes, reproduce the manifest digest, and verify the auditor Ed25519 signature.</p></article><article><span>02</span><b>PIN ISSUER</b><p>Require the credential issuer key and minimum registry version to match a locally configured trust root.</p></article><article><span>03</span><b>BIND SUBJECT</b><p>Match auditor, organization, raw key, key digest, and requested certification target exactly.</p></article><article><span>04</span><b>CHECK LIFECYCLE</b><p>Require active, valid, unrevoked status whose issuer-signed freshness window covers the audit signing time.</p></article></div></div></section><footer className="wantedFooter"><div className="shell"><div className="brand"><span className="brandMark">EA</span><span>EMBODIED <b>ARENA</b> / AUDIT TRUST 0.2-V2</span></div><p>A cryptographic trust chain for every WANTED certification claim.</p><a href="/wanted-10k/audit">OPEN AUDIT PACK →</a></div></footer></main>}

import type { Metadata } from "next";
import { SiteNav } from "../../components/SiteNav";
import { HumanMeasuresLab } from "./HumanMeasuresLab";
import { HUMAN_PHASES, HUMAN_QUESTIONS } from "./profile.ts";

export const metadata:Metadata={title:"Four-Item Human Measures — WANTED-10K",description:"Verify randomized, neutral, aggregate-only collection of keep, value, burden, and trust measures across a robot's residence.",alternates:{canonical:"/wanted-10k/human-measures"}};

export default function HumanMeasuresPage(){return <main className="humanPage"><SiteNav/><section className="humanHero shell"><span className="eyebrow"><i className="liveDot"/> HUMAN MEASURES · PROFILE 0.2-H1</span><h1>Ask four things.<br/><em>Do not manufacture an answer.</em></h1><p>At preregistered randomized intervals, participants answer one tiny instrument: keep, value, burden, and trust. Every due prompt remains in the denominator, nonresponse stays visible, and the result never enters W.</p><div className="humanActions"><a className="primary" href="#reproducer">Verify a study <span>↓</span></a><a className="secondary" href="/wanted-10k/human-measures.template.json">Download template</a><a className="secondary" href="/wanted-10k/human-measures.json">Open contract</a></div><div className="humanProof"><div><b>4</b><span>EXACT QUESTIONS</span></div><div><b>5</b><span>RESIDENCE PHASES</span></div><div><b>72H</b><span>RESPONSE WINDOW</span></div><div><b>0</b><span>RANKING EFFECT</span></div></div></section><section className="humanInstrument"><div className="shell"><div className="sectionHead wantedHead"><div><span className="kicker">01 / CANONICAL INSTRUMENT</span><h2>Tiny by design.<br/><em>Exact by necessity.</em></h2></div><p>Changing the wording, scales, timing, or response rules after seeing results breaks comparability. All four answers travel together as one prompt set.</p></div><div className="questionGrid">{HUMAN_QUESTIONS.map((question,index)=><article key={question.id}><span>Q{index+1} / {question.id.toUpperCase()}</span><h3>{question.prompt}</h3><div>{question.values.map(value=><i key={String(value)}>{String(value).toUpperCase()}</i>)}</div></article>)}</div></div></section><section className="humanSchedule"><div className="shell"><div><span className="kicker">02 / LONGITUDINAL VIEW</span><h2>Random moments.<br/><em>Fixed analysis.</em></h2><p>The schedule is frozen before hour one. Delivery can drift by at most 24 hours, participants have 72 hours to respond, and one neutral reminder is the maximum.</p></div><div className="phaseRail">{HUMAN_PHASES.map((phase,index)=><article key={phase.id}><span>0{index+1}</span><b>{phase.label}</b><small>{phase.upper>10000?`${phase.lower}–10,000H`:`${phase.lower}–${phase.upper}H`}</small></article>)}</div></div></section><section id="reproducer" className="humanReproducer"><div className="shell"><div className="sectionHead wantedHead"><div><span className="kicker">03 / LOCAL REPRODUCTION</span><h2>Keep every prompt.<br/><em>Rebuild every phase.</em></h2></div><p>The checker validates schedule completeness, neutral collection, exact scales, explicit nonresponse, evidence bindings, and all overall and phase-level summaries.</p></div><HumanMeasuresLab/></div></section><section className="humanBoundary"><div className="shell"><div><b>BEHAVIORAL PRIMARY</b><span>Permanent voluntary rejection</span><p>Determines W and public rank.</p></div><i>≠</i><div><b>ATTITUDINAL DIAGNOSTIC</b><span>Keep · value · burden · trust</span><p>Explains the trajectory; never changes W.</p></div></div></section><footer className="wantedFooter"><div className="shell"><div className="brand"><span className="brandMark">EA</span><span>EMBODIED <b>ARENA</b> / HUMAN MEASURES 0.2-H1</span></div><p>Four questions, asked neutrally over time.</p><a href="/wanted-10k">BACK TO BENCHMARK -&gt;</a></div></footer></main>}

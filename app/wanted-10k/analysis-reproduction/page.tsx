import type { Metadata } from "next";import { SiteNav } from "../../components/SiteNav";import { AnalysisReproductionLab } from "./AnalysisReproductionLab";
export const metadata:Metadata={title:"Analysis Reproduction - WANTED-10K",description:"Reproduce the ranked WANTED Score, uncertainty, and robustness from an audited environment-level endpoint table.",alternates:{canonical:"/wanted-10k/analysis-reproduction"}};
const gates=[["A1","FROZEN ESTIMAND","One estimator, horizon, event, and tie rule."],["A2","ENDPOINT MAPPING","Every exit becomes one declared disposition."],["A3","UNIQUE UNITS","One hashed environment per analysis row."],["A4","WILD SUPPORT","N≥20 and at least 10,000 resident hours."],["A5","PRIMARY RESULT","Rebuild W and S(10K) exactly."],["A6","UNCERTAINTY","Rebuild CI, bounds, influence, and support."],["A7","INDEPENDENT AUDIT","Bind inputs, code, log, and assessor."]];
export default function AnalysisReproductionPage(){return <main className="analysisPage"><SiteNav/><section className="analysisHero shell"><span className="eyebrow"><i className="liveDot"/> ANALYSIS REPRODUCTION - 0.2-A2</span><h1>Do not trust W.<br/><em>Rebuild it.</em></h1><p>A leaderboard row cannot rest on a self-reported score. Profile 0.2-A2 starts from one audited row per environment, then reproduces the primary RMST, deterministic bootstrap interval, censoring envelope, influence, tail support, and exact-horizon accounting under one frozen algorithm.</p><div className="analysisActions"><a className="primary" href="#evaluator">Reproduce a claim <span>-&gt;</span></a><a className="secondary" href="/wanted-10k/analysis-reproduction.template.json">Download template</a><a className="secondary" href="/wanted-10k/analysis-reproduction.json">Open contract</a></div><div className="analysisProof"><div><b>1</b><span>CANONICAL ESTIMATOR</span></div><div><b>10K</b><span>BOOTSTRAP DRAWS</span></div><div><b>7</b><span>REPRODUCED OUTPUTS</span></div><div><b>7</b><span>HARD GATES</span></div></div></section><section className="analysisGateSection"><div className="shell"><div className="sectionHead wantedHead"><div><span className="kicker">01 / CLAIM VERIFICATION</span><h2>Input rows first.<br/><em>Leaderboard second.</em></h2></div><p>Every displayed aggregate must reconcile to the same environment-level endpoint table and upstream evidence commitments.</p></div><div className="analysisGateGrid">{gates.map(([id,title,copy])=><article key={id}><span>{id}</span><b>{title}</b><p>{copy}</p><i>REQUIRED</i></article>)}</div></div></section><section className="analysisFormula"><div className="shell"><div><span className="kicker">02 / DETERMINISTIC PIPELINE</span><h2>Same rows.<br/><em>Same answer.</em></h2><p>The bootstrap is stochastic in method but deterministic in verification: a frozen seed, PCG32 stream, high-word index mapping, and percentile interpolation define every resample.</p></div><div className="analysisEquation"><span>PRIMARY + UNCERTAINTY</span><b>rows → KM → RMST → W<br/>rows × PCG32 → 95% CI</b><p>Any implementation may be used, but the official submission must reproduce the canonical outputs within 10⁻⁶.</p></div></div></section><section className="analysisOutputs"><div className="shell"><div className="sectionHead wantedHead"><div><span className="kicker">03 / REQUIRED TRACE</span><h2>Publish the result.<br/><em>Publish its fragility.</em></h2></div><p>W alone is insufficient. The same manifest carries censoring sensitivity, leave-one-environment-out influence, and observed support at the 10,000-hour tail.</p></div><div className="analysisOutputGrid"><article><b>PRIMARY</b><p>W and S(10K), with refusal when the horizon is unsupported.</p></article><article><b>UNCERTAINTY</b><p>10,000 environment-level percentile bootstrap draws and valid fraction.</p></article><article><b>ROBUSTNESS</b><p>Pessimistic and optimistic censoring bounds plus maximum leave-one-out shift.</p></article><article><b>SUPPORT</b><p>Pre-event risk set at 10K, exact-horizon rejections, retained completions, early exits, and endpoint totals.</p></article></div></div></section><section id="evaluator" className="analysisEvaluator"><div className="shell"><div className="sectionHead wantedHead"><div><span className="kicker">04 / LOCAL REPRODUCER</span><h2>Paste the claim.<br/><em>Compute the truth.</em></h2></div><p>The checker reruns every official calculation locally and compares the claimed values to the reproduced values without uploading the endpoint table.</p></div><AnalysisReproductionLab/></div></section><section className="analysisCallout"><div className="shell"><b>REPRODUCTION != CERTIFICATION</b><p>A matching statistic still requires cohort integrity, exposure integrity, safety, preregistration, and independent source verification before registry admission.</p><a href="/wanted-10k/protocol">OPEN PROTOCOL KIT -&gt;</a></div></section><footer className="wantedFooter"><div className="shell"><div className="brand"><span className="brandMark">EA</span><span>EMBODIED <b>ARENA</b> / ANALYSIS REPRODUCTION</span></div><p>The executable proof beneath every ranked W.</p><a href="/wanted-10k">BACK TO BENCHMARK -&gt;</a></div></footer></main>}

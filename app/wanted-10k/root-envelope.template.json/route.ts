import { sampleBundle } from "../conformance/validator.ts";
import { createRootEnvelope } from "../root-envelope/profile.ts";

export async function GET(){const sample=await sampleBundle(),events=sample.jsonl.split("\n").map(line=>JSON.parse(line)),key_manifest=JSON.parse(sample.keyManifest),activation_at="2026-08-28T18:00:00.000Z",observation_end_at="2026-08-28T20:00:00.000Z",first=await createRootEnvelope({events,keyManifest:key_manifest,rootOrdinal:0,coversThroughAt:"2026-08-28T19:00:00.000Z"}),second=await createRootEnvelope({events,keyManifest:key_manifest,rootOrdinal:1,coversThroughAt:observation_end_at,previousEnvelope:first});return Response.json({profile_version:"0.2-RE1",notice:"Synthetic interoperability example. Real root envelopes remain inside operator-approved evidence infrastructure.",activation_at,observation_end_at,commitment_interval_hours:1,key_manifest,events,envelopes:[first,second],root_witness_projection:[first,second].map(root=>({root_ordinal:root.root_ordinal,covers_through_at:root.covers_through_at,root_commitment_sha256:root.root_commitment_sha256}))},{headers:{"cache-control":"public, max-age=3600"}});}

import{experimentRolloutPackageVerifierSource}from"./rollout-package-verifier-source.ts";
import{EXPERIMENT_ROLLOUT_AUTHORIZATION_BUNDLE_PROFILE,EXPERIMENT_ROLLOUT_AUTHORIZATION_CLI_EXIT_CODES,EXPERIMENT_ROLLOUT_AUTHORIZATION_MAX_VALIDITY_HOURS,EXPERIMENT_ROLLOUT_AUTHORIZATION_PROFILE,EXPERIMENT_ROLLOUT_AUTHORITY_KEY_MANIFEST_SHA256,EXPERIMENT_ROLLOUT_AUTHORITY_KEY_PROFILE,EXPERIMENT_ROLLOUT_REFERENCE_APPROVAL_SHA256,EXPERIMENT_ROLLOUT_REFERENCE_PACKAGE_SHA256,EXPERIMENT_ROLLOUT_REFERENCE_PLAN_SHA256,experimentRolloutAuthorizationReferenceBundle}from"./rollout-authorization.ts";
const bytes=new TextEncoder().encode(experimentRolloutPackageVerifierSource),embeddedRolloutSource=btoa(Array.from(bytes,byte=>String.fromCharCode(byte)).join("")),reference=experimentRolloutAuthorizationReferenceBundle();
export const experimentRolloutAuthorizationVerifierSource=String.raw`// WANTED signed artifact-bound rollout authorization ${EXPERIMENT_ROLLOUT_AUTHORIZATION_PROFILE}
const rolloutSdk=await import(${JSON.stringify(`data:text/javascript;base64,${embeddedRolloutSource}`)});
export const PROFILE=${JSON.stringify(EXPERIMENT_ROLLOUT_AUTHORIZATION_PROFILE)},BUNDLE_PROFILE=${JSON.stringify(EXPERIMENT_ROLLOUT_AUTHORIZATION_BUNDLE_PROFILE)},KEY_PROFILE=${JSON.stringify(EXPERIMENT_ROLLOUT_AUTHORITY_KEY_PROFILE)},CLI_EXIT_CODES=Object.freeze(${JSON.stringify(EXPERIMENT_ROLLOUT_AUTHORIZATION_CLI_EXIT_CODES)}),REFERENCE_BUNDLE=Object.freeze(${JSON.stringify(reference)}),MAX_VALIDITY_HOURS=${EXPERIMENT_ROLLOUT_AUTHORIZATION_MAX_VALIDITY_HOURS},TRUST_ROOT=${JSON.stringify(EXPERIMENT_ROLLOUT_AUTHORITY_KEY_MANIFEST_SHA256)},APPROVAL_DIGEST=${JSON.stringify(EXPERIMENT_ROLLOUT_REFERENCE_APPROVAL_SHA256)},PACKAGE_DIGEST=${JSON.stringify(EXPERIMENT_ROLLOUT_REFERENCE_PACKAGE_SHA256)},PLAN_DIGEST=${JSON.stringify(EXPERIMENT_ROLLOUT_REFERENCE_PLAN_SHA256)},CANDIDATE_DIGEST="f5c915b3a05b1839a699d0683f0c72a1af83561506c20c92d50e36a310c0e974";
const BK=["key_manifest","profile","receipt","rollout_package","trusted_key_manifest_sha256","verification_time"],RK=["approval_id","approval_profile","authorization_id","authorization_purpose","authorization_record_uri","authorized_action","authorized_at","automatic_deployment","automatic_progression","candidate_manifest_sha256","canonical_approval_bundle_sha256","canonical_rollout_package_sha256","expires_at","key_id","profile","rollout_authority","rollout_authority_role","rollout_package_profile","rollout_plan_sha256","signature_algorithm","signature_base64url","target_analysis_cohort","target_rotator_version"],UK=RK.filter(k=>k!=="signature_base64url"),MK=["keys","profile","rollout_authority","rollout_authority_uri"],KK=["algorithm","key_id","public_key_jwk","purpose","revoked_at","valid_from","valid_until"],JK=["crv","kty","x"],exact=(v,k)=>!!v&&typeof v==="object"&&!Array.isArray(v)&&JSON.stringify(Object.keys(v).sort())===JSON.stringify([...k].sort()),utc=v=>typeof v==="string"&&/^\d{4}-\d\d-\d\dT\d\d:\d\d:\d\dZ$/.test(v)&&Number.isFinite(Date.parse(v)),digest=v=>typeof v==="string"&&/^[0-9a-f]{64}$/.test(v),https=v=>typeof v==="string"&&/^https:\/\/[A-Za-z0-9]/.test(v),b64=(v,n)=>typeof v==="string"&&new RegExp("^[A-Za-z0-9_-]{"+n+"}$").test(v),decode=v=>Uint8Array.from(atob(v.replace(/-/g,"+").replace(/_/g,"/").padEnd(Math.ceil(v.length/4)*4,"=")),c=>c.charCodeAt(0)),encode=v=>btoa(String.fromCharCode(...v)).replace(/=/g,"").replace(/\+/g,"-").replace(/\//g,"_");
export const canonicalize=rolloutSdk.canonicalize,sha256Canonical=rolloutSdk.sha256Canonical;export function unsignedReceipt(v){const r={...v};delete r.signature_base64url;return r}export function signingBytes(v){return new TextEncoder().encode(canonicalize(unsignedReceipt(v)))}
const failure=errors=>({profile:PROFILE,status:"fail",authorization_id:null,authorized_action:null,target_rotator_version:null,target_analysis_cohort:null,calculated_approval_bundle_sha256:null,calculated_rollout_package_sha256:null,calculated_rollout_plan_sha256:null,calculated_key_manifest_sha256:null,rollout_package_verified:false,approval_digest_verified:false,package_digest_verified:false,candidate_digest_verified:false,plan_digest_verified:false,trust_root_digest_verified:false,authority_key_verified:false,signature_verified:false,chronology_consistent:false,within_validity_window:false,authorization_envelope_verified:false,production_rollout_review_authorized:false,automatic_progression:false,deployment_authorized:false,errors,limitations:["Malformed rollout authorization cannot support review."]});
export async function verifyRolloutAuthorization(bundle){if(!exact(bundle,BK)||bundle.profile!==BUNDLE_PROFILE)return failure(["Rollout-authorization bundle fields or profile do not match the frozen contract."]);const r=bundle.receipt,m=bundle.key_manifest,p=bundle.rollout_package,errors=[];let ad=null,pd=null,rd=null,md=null,keyOk=false,sig=false;try{rolloutSdk.canonicalize(bundle)}catch(error){errors.push(error instanceof Error?error.message:"Rollout authorization is not strict I-JSON.")}if(!exact(r,RK)||r.profile!==PROFILE)errors.push("Rollout-authorization receipt fields or profile are invalid.");if(!exact(m,MK)||m.profile!==KEY_PROFILE)errors.push("Rollout-authority key manifest is invalid.");if(!["production","conformance_only"].includes(r?.authorization_purpose))errors.push("Authorization purpose is invalid.");if(typeof r?.authorization_id!=="string"||r.authorization_id.length<8||!https(r?.authorization_record_uri)||r?.rollout_authority!==m?.rollout_authority||r?.rollout_authority_role!=="accountable_site_owner")errors.push("Rollout-authorization identity or authority is invalid.");if(r?.approval_profile!=="0.39-DAR1"||r?.rollout_package_profile!=="0.40-RP1"||r?.authorized_action!=="stage_proof"||r?.target_rotator_version!=="0.37-R37"||r?.target_analysis_cohort!=="wanted_landing_v1-C9"||r?.automatic_progression!==false||r?.automatic_deployment!==false)errors.push("Rollout-authorization scope or target is invalid.");if(!utc(r?.authorized_at)||!utc(r?.expires_at)||!utc(bundle.verification_time)||![r?.canonical_approval_bundle_sha256,r?.canonical_rollout_package_sha256,r?.candidate_manifest_sha256,r?.rollout_plan_sha256,bundle.trusted_key_manifest_sha256].every(digest)||r?.signature_algorithm!=="Ed25519"||!b64(r?.signature_base64url,86))errors.push("Authorization chronology, digest, signature, or trust-root encoding is invalid.");try{ad=await sha256Canonical(p?.approval_bundle);pd=await sha256Canonical(p);rd=await sha256Canonical(p?.rollout_plan);md=await sha256Canonical(m)}catch{errors.push("Canonical digest calculation failed.")}const pr=await rolloutSdk.verifyRolloutPackage(p),pv=pr.status==="pass";if(!pv)errors.push("Embedded artifact-bound rollout package failed verification.");const av=ad===r?.canonical_approval_bundle_sha256&&r?.canonical_approval_bundle_sha256===APPROVAL_DIGEST;if(!av)errors.push("Authorization does not bind the exact approved decision bundle.");const pgv=pd===r?.canonical_rollout_package_sha256&&r?.canonical_rollout_package_sha256===PACKAGE_DIGEST;if(!pgv)errors.push("Authorization does not bind the exact rollout package.");const cv=r?.candidate_manifest_sha256===p?.candidate_manifest_sha256&&r?.candidate_manifest_sha256===CANDIDATE_DIGEST;if(!cv)errors.push("Authorization does not bind the frozen candidate manifest.");const rv=rd===r?.rollout_plan_sha256&&r?.rollout_plan_sha256===PLAN_DIGEST;if(!rv)errors.push("Authorization does not bind the exact staged-rollout plan.");const tv=md===bundle.trusted_key_manifest_sha256&&bundle.trusted_key_manifest_sha256===TRUST_ROOT;if(!tv)errors.push("Rollout-authority key manifest does not match the pinned digest.");if(r?.approval_id!==p?.approval_bundle?.receipt?.approval_id||r?.authorized_action!==pr.authorized_action)errors.push("Authorization identity or action does not match the verified approval and package.");const keys=Array.isArray(m?.keys)?m.keys:[];if(keys.length!==1)errors.push("Rollout-authority key manifest must contain exactly one conformance key.");const key=keys.find(k=>k?.key_id===r?.key_id);if(!key||!exact(key,KK)||!exact(key.public_key_jwk,JK))errors.push("Rollout-authority key is missing or malformed.");else{const j=key.public_key_jwk,authorized=Date.parse(String(r.authorized_at)),from=Date.parse(String(key.valid_from)),until=Date.parse(String(key.valid_until));keyOk=key.algorithm==="Ed25519"&&key.purpose==="artifact_bound_staged_rollout_authorization"&&key.revoked_at===null&&utc(key.valid_from)&&utc(key.valid_until)&&authorized>=from&&authorized<=until&&j.kty==="OKP"&&j.crv==="Ed25519"&&b64(j.x,43);if(!keyOk)errors.push("Rollout-authority key lifecycle or purpose is invalid.");if(keyOk&&b64(r.signature_base64url,86))try{const pk=await crypto.subtle.importKey("jwk",j,{name:"Ed25519"},false,["verify"]);sig=await crypto.subtle.verify("Ed25519",pk,decode(r.signature_base64url),signingBytes(r))}catch{sig=false}}if(!sig)errors.push("Rollout-authority Ed25519 signature did not verify.");const approvalTime=Date.parse(String(p?.approval_bundle?.receipt?.approved_at)),authorized=Date.parse(String(r?.authorized_at)),expires=Date.parse(String(r?.expires_at)),verification=Date.parse(String(bundle.verification_time)),chrono=utc(p?.approval_bundle?.receipt?.approved_at)&&authorized>=approvalTime&&expires>authorized&&expires-authorized<=MAX_VALIDITY_HOURS*3600000;if(!chrono)errors.push("Artifact authorization must follow decision approval and expire within 24 hours.");const active=utc(bundle.verification_time)&&verification>=authorized&&verification<=expires;if(!active)errors.push("Artifact authorization is not valid at the declared verification time.");const passed=errors.length===0,production=passed&&r.authorization_purpose==="production"&&pr.execution_eligible===true;return{profile:PROFILE,status:passed?"pass":"fail",authorization_id:typeof r?.authorization_id==="string"?r.authorization_id:null,authorized_action:typeof r?.authorized_action==="string"?r.authorized_action:null,target_rotator_version:typeof r?.target_rotator_version==="string"?r.target_rotator_version:null,target_analysis_cohort:typeof r?.target_analysis_cohort==="string"?r.target_analysis_cohort:null,calculated_approval_bundle_sha256:ad,calculated_rollout_package_sha256:pd,calculated_rollout_plan_sha256:rd,calculated_key_manifest_sha256:md,rollout_package_verified:pv,approval_digest_verified:av,package_digest_verified:pgv,candidate_digest_verified:cv,plan_digest_verified:rv,trust_root_digest_verified:tv,authority_key_verified:keyOk,signature_verified:sig,chronology_consistent:chrono,within_validity_window:active,authorization_envelope_verified:passed,production_rollout_review_authorized:production,automatic_progression:false,deployment_authorized:false,errors,limitations:["The reference authority, approval, artifact, digests, and authorization are synthetic.","The verifier receives no source-archive bytes and cannot authenticate repository provenance.","A valid production envelope permits human rollout review only and never advances phases, changes allocation, publishes, or deploys."]}}
export async function prepareRolloutAuthorization(unsigned,keyManifest,rolloutPackage,trustedDigest,verificationTime){if(!exact(unsigned,UK))throw new TypeError("Unsigned rollout-authorization receipt fields are invalid.");const provisional={profile:BUNDLE_PROFILE,receipt:{...unsigned,signature_base64url:"A".repeat(86)},key_manifest:keyManifest,rollout_package:rolloutPackage,trusted_key_manifest_sha256:trustedDigest,verification_time:verificationTime},result=await verifyRolloutAuthorization(provisional),remaining=result.errors.filter(e=>e!=="Rollout-authority Ed25519 signature did not verify.");if(remaining.length)throw new TypeError(remaining.join(" "));const bytes=signingBytes(unsigned);return{profile:PROFILE,algorithm:"Ed25519",canonicalization:"sorted_key_recursive_I-JSON",unsigned_receipt:unsigned,signing_bytes_base64url:encode(bytes),signing_bytes_sha256:await crypto.subtle.digest("SHA-256",bytes).then(hash=>Array.from(new Uint8Array(hash),b=>b.toString(16).padStart(2,"0")).join("")),private_key_boundary:"signing_key_remains_in_caller_owned_HSM_TPM_KMS_or_secure_enclave"}}
export async function attachRolloutAuthorizationSignature(unsigned,signature,keyManifest,rolloutPackage,trustedDigest,verificationTime){if(!b64(signature,86))throw new TypeError("A 64-byte Ed25519 signature is required.");const bundle={profile:BUNDLE_PROFILE,receipt:{...unsigned,signature_base64url:signature},key_manifest:keyManifest,rollout_package:rolloutPackage,trusted_key_manifest_sha256:trustedDigest,verification_time:verificationTime},result=await verifyRolloutAuthorization(bundle);if(result.status!=="pass")throw new TypeError(result.errors.join(" "));return bundle}export async function signRolloutAuthorization(unsigned,sign,keyManifest,rolloutPackage,trustedDigest,verificationTime){const prepared=await prepareRolloutAuthorization(unsigned,keyManifest,rolloutPackage,trustedDigest,verificationTime),signature=await sign(decode(prepared.signing_bytes_base64url));if(!(signature instanceof Uint8Array)||signature.length!==64)throw new TypeError("Secure signer must return exactly 64 Ed25519 signature bytes.");return attachRolloutAuthorizationSignature(unsigned,encode(signature),keyManifest,rolloutPackage,trustedDigest,verificationTime)}
export async function runCli(args=[],io={}){const out=io.stdout||((v)=>console.log(v)),err=io.stderr||((v)=>console.error(v)),read=io.readFile||(async p=>(await import("node:fs/promises")).readFile(p,"utf8")),json=async p=>JSON.parse(await read(p)),usage="Usage:\n  node wanted-rollout-authorization.mjs --prepare BUNDLE.json\n  node wanted-rollout-authorization.mjs --attach BUNDLE.json SIGNATURE.txt\n  node wanted-rollout-authorization.mjs --verify BUNDLE.json\n  node wanted-rollout-authorization.mjs --conformance";try{if(args.length===1&&args[0]==="--conformance"){const r=await verifyRolloutAuthorization(REFERENCE_BUNDLE);out(JSON.stringify(r,null,2));return r.status==="pass"?0:1}if(args.length===2&&args[0]==="--verify"){const r=await verifyRolloutAuthorization(await json(args[1]));out(JSON.stringify(r,null,2));return r.status==="pass"?0:1}if(args.length===2&&args[0]==="--prepare"){const b=await json(args[1]),r=await prepareRolloutAuthorization(unsignedReceipt(b.receipt),b.key_manifest,b.rollout_package,b.trusted_key_manifest_sha256,b.verification_time);out(JSON.stringify(r,null,2));return 0}if(args.length===3&&args[0]==="--attach"){const b=await json(args[1]),sig=(await read(args[2])).trim(),r=await attachRolloutAuthorizationSignature(unsignedReceipt(b.receipt),sig,b.key_manifest,b.rollout_package,b.trusted_key_manifest_sha256,b.verification_time);out(JSON.stringify(r,null,2));return 0}err(usage);return 2}catch(error){err(String(error?.message||error));return 1}}
const normalized=v=>v.replaceAll("\\","/").replace(/^\/([A-Za-z]:\/)/,"$1").toLowerCase();if(typeof process!=="undefined"&&process.argv?.[1]&&import.meta.url.startsWith("file:")&&normalized(decodeURIComponent(new URL(import.meta.url).pathname))===normalized(process.argv[1]))process.exitCode=await runCli(process.argv.slice(2));`;
export const experimentRolloutAuthorizationVerifierContract={name:"Embodied Arena WANTED Rollout Authorization Helper",version:EXPERIMENT_ROLLOUT_AUTHORIZATION_PROFILE,bundle_profile:EXPERIMENT_ROLLOUT_AUTHORIZATION_BUNDLE_PROFILE,module:"/experiments/wanted-rollout-authorization.mjs",contract:"/experiments/rollout-authorization.json",schema:"/experiments/rollout-authorization.schema.json",reference_bundle:"/experiments/rollout-authorization.reference.json",key_manifest:"/experiments/rollout-authorization-keys.json",lab:"/experiments/rollout-lab",format:"JavaScript ESM",runtime_dependencies:0,performs_network_requests:false,private_key_handling:"never_accepts_reads_stores_or_exports_private_keys",exports:["canonicalize","sha256Canonical","unsignedReceipt","signingBytes","verifyRolloutAuthorization","prepareRolloutAuthorization","attachRolloutAuthorizationSignature","signRolloutAuthorization","CLI_EXIT_CODES","runCli"],cli:{runtime:"Node.js 22+",modes:{prepare:"emit canonical signing bytes and digest",attach:"attach and verify an external signature",verify:"verify a signed authorization",conformance:"verify the synthetic reference bundle"},private_key_input_supported:false,exit_codes:EXPERIMENT_ROLLOUT_AUTHORIZATION_CLI_EXIT_CODES},automatic_progression:false,changes_live_allocation:false,deploys:false,interpretation:"no-key-custody signature over the exact approval, candidate, package, plan, and C9 target; verification never executes rollout"}as const;

import { experimentRotatorSdkSource } from "./rotator-sdk-source.ts";
import { experimentRolloutSimulatorVerifierSource } from "./rollout-simulator-verifier-source.ts";
import {
  EXPERIMENT_ROLLOUT_CANDIDATE_SOURCE_SHA256,
  EXPERIMENT_ROLLOUT_COHORT_SEPARATION_BUNDLE_PROFILE,
  EXPERIMENT_ROLLOUT_COHORT_SEPARATION_CERTIFICATE_PROFILE,
  EXPERIMENT_ROLLOUT_COHORT_SEPARATION_CLI_EXIT_CODES,
  EXPERIMENT_ROLLOUT_COHORT_SEPARATION_PROFILE,
  EXPERIMENT_ROLLOUT_PREVIOUS_SOURCE_SHA256,
} from "./rollout-cohort-separation.ts";

const encode = (source: string) => {
  const bytes = new TextEncoder().encode(source);
  return btoa(Array.from(bytes, (byte) => String.fromCharCode(byte)).join(""));
};
const previousSourceBase64 = encode(experimentRotatorSdkSource);
const candidateSourceBase64 = encode(experimentRolloutSimulatorVerifierSource);

export const experimentRolloutCohortSeparationVerifierSource = String.raw`// WANTED C8-to-C9 cohort separation review ${EXPERIMENT_ROLLOUT_COHORT_SEPARATION_PROFILE}
const PREVIOUS_SOURCE_BASE64=${JSON.stringify(previousSourceBase64)},CANDIDATE_SOURCE_BASE64=${JSON.stringify(candidateSourceBase64)},decodeSource=value=>new TextDecoder().decode(Uint8Array.from(atob(value),character=>character.charCodeAt(0))),previousSource=decodeSource(PREVIOUS_SOURCE_BASE64),candidateSource=decodeSource(CANDIDATE_SOURCE_BASE64),previousSdk=await import("data:text/javascript;base64,"+PREVIOUS_SOURCE_BASE64),candidateSdk=await import("data:text/javascript;base64,"+CANDIDATE_SOURCE_BASE64);
export const PROFILE=${JSON.stringify(EXPERIMENT_ROLLOUT_COHORT_SEPARATION_PROFILE)},CERTIFICATE_PROFILE=${JSON.stringify(EXPERIMENT_ROLLOUT_COHORT_SEPARATION_CERTIFICATE_PROFILE)},BUNDLE_PROFILE=${JSON.stringify(EXPERIMENT_ROLLOUT_COHORT_SEPARATION_BUNDLE_PROFILE)},CLI_EXIT_CODES=Object.freeze(${JSON.stringify(EXPERIMENT_ROLLOUT_COHORT_SEPARATION_CLI_EXIT_CODES)}),PREVIOUS_SOURCE_SHA256=${JSON.stringify(EXPERIMENT_ROLLOUT_PREVIOUS_SOURCE_SHA256)},CANDIDATE_SOURCE_SHA256=${JSON.stringify(EXPERIMENT_ROLLOUT_CANDIDATE_SOURCE_SHA256)};
const KEYS=["candidate_analysis_cohort","candidate_source_profile","candidate_source_sha256","index_start","previous_analysis_cohort","previous_source_profile","previous_source_sha256","profile","purpose","sample_size"],exact=(value,keys)=>!!value&&typeof value==="object"&&!Array.isArray(value)&&JSON.stringify(Object.keys(value).sort())===JSON.stringify([...keys].sort()),canonical=value=>{if(value===null||typeof value!=="object")return JSON.stringify(value)??"null";if(Array.isArray(value))return"["+value.map(canonical).join(",")+"]";return"{"+Object.keys(value).sort().map(key=>JSON.stringify(key)+":"+canonical(value[key])).join(",")+"}"},digest=async value=>Array.from(new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(value))),byte=>byte.toString(16).padStart(2,"0")).join(""),sha256=value=>digest(canonical(value)),ppm=value=>Math.round(value*1000000);
export function referenceBundle(){return{profile:BUNDLE_PROFILE,purpose:"conformance_only",sample_size:1000000,index_start:0,previous_analysis_cohort:"wanted_landing_v1-C8",candidate_analysis_cohort:"wanted_landing_v1-C9",previous_source_profile:"0.32-RSDK1",previous_source_sha256:PREVIOUS_SOURCE_SHA256,candidate_source_profile:"0.43-RSM1",candidate_source_sha256:CANDIDATE_SOURCE_SHA256}}
export async function auditCohortSeparation(bundle){const errors=[],findings=[],shape=exact(bundle,KEYS)&&bundle.profile===BUNDLE_PROFILE&&bundle.purpose==="conformance_only";if(!shape)errors.push("Cohort-separation bundle fields, profile, or purpose are invalid.");const sample=bundle?.sample_size===1000000&&bundle?.index_start===0;if(!sample)errors.push("The audit must enumerate synthetic indices 0 through 999,999 exactly once.");const identity=bundle?.previous_analysis_cohort==="wanted_landing_v1-C8"&&bundle?.candidate_analysis_cohort==="wanted_landing_v1-C9";if(!identity)errors.push("The previous and candidate cohort identities are invalid.");const previousDigest=await digest(previousSource),candidateDigest=await digest(candidateSource),source=bundle?.previous_source_profile==="0.32-RSDK1"&&bundle?.previous_source_sha256===PREVIOUS_SOURCE_SHA256&&previousDigest===PREVIOUS_SOURCE_SHA256&&bundle?.candidate_source_profile==="0.43-RSM1"&&bundle?.candidate_source_sha256===CANDIDATE_SOURCE_SHA256&&candidateDigest===CANDIDATE_SOURCE_SHA256;if(!source)errors.push("The audit is not bound to the exact previous and candidate implementations.");let certificate=null,deterministic=false,separation=false;if(shape&&sample&&identity&&source){const contingency={control:[0,0],proof:[0,0],developer:[0,0]},deciles=Array.from({length:10},()=>Array(10).fill(0)),residues=Array(16).fill(0);let same=0,sumOld=0,sumNew=0,sumOldSquared=0,sumNewSquared=0,sumProduct=0;for(let index=0;index<1000000;index++){const unitId=candidateSdk.syntheticUnitId(index),previous=previousSdk.assignWantedVariant(unitId),candidateBucket=candidateSdk.rolloutBucket(unitId),column=candidateBucket<2500?1:0;contingency[previous.variant][column]++;deciles[Math.floor(previous.bucket/1000)][Math.floor(candidateBucket/1000)]++;residues[((candidateBucket-previous.bucket)%16+16)%16]++;if(previous.bucket===candidateBucket)same++;sumOld+=previous.bucket;sumNew+=candidateBucket;sumOldSquared+=previous.bucket**2;sumNewSquared+=candidateBucket**2;sumProduct+=previous.bucket*candidateBucket}if(residues.reduce((sum,count)=>sum+count,0)!==1000000)throw new Error("Residue accounting must retain every synthetic unit.");const variants=["control","proof","developer"],variantTotals=variants.map(variant=>contingency[variant][0]+contingency[variant][1]),candidateTotals=[variants.reduce((sum,variant)=>sum+contingency[variant][0],0),variants.reduce((sum,variant)=>sum+contingency[variant][1],0)];let treatmentChi=0;variants.forEach((variant,row)=>{for(let column=0;column<2;column++){const expected=variantTotals[row]*candidateTotals[column]/1000000;treatmentChi+=(contingency[variant][column]-expected)**2/expected}});const rowTotals=deciles.map(row=>row.reduce((sum,count)=>sum+count,0)),columnTotals=Array.from({length:10},(_,column)=>deciles.reduce((sum,row)=>sum+row[column],0));let decileChi=0;for(let row=0;row<10;row++)for(let column=0;column<10;column++){const expected=rowTotals[row]*columnTotals[column]/1000000;decileChi+=(deciles[row][column]-expected)**2/expected}const numerator=1000000*sumProduct-sumOld*sumNew,denominator=Math.sqrt((1000000*sumOldSquared-sumOld**2)*(1000000*sumNewSquared-sumNew**2)),treatmentPpm=ppm(Math.sqrt(treatmentChi/1000000)),decilePpm=ppm(Math.sqrt(decileChi/9000000)),correlationPpm=ppm(numerator/denominator),observedResidues=residues.flatMap((count,residue)=>count>0?[residue]:[]),gates={treatment_association_at_most_5_000_ppm:treatmentPpm<=5000,decile_association_at_most_5_000_ppm:decilePpm<=5000,absolute_linear_correlation_at_most_5_000_ppm:Math.abs(correlationPpm)<=5000,all_modulo_16_difference_residues_observed:observedResidues.length===16,exact_bucket_match_count_between_50_and_150:same>=50&&same<=150};separation=Object.values(gates).every(Boolean);if(!gates.all_modulo_16_difference_residues_observed)findings.push("Only odd modulo-16 bucket differences occur; the candidate and previous FNV domains retain a deterministic low-bit relation.");if(!gates.exact_bucket_match_count_between_50_and_150)findings.push("No exact bucket matches occur in one million units; an independent 10,000-bucket remap would produce approximately 100.");const table={control:{candidate_control:contingency.control[0],candidate_proof:contingency.control[1]},proof:{candidate_control:contingency.proof[0],candidate_proof:contingency.proof[1]},developer:{candidate_control:contingency.developer[0],candidate_proof:contingency.developer[1]}},diagnostics={previous_variant_by_candidate_ramp:table,decile_contingency:deciles,modulo_16_difference_residue_counts:residues,exact_bucket_match_count:same},unsigned={profile:CERTIFICATE_PROFILE,purpose:"conformance_only",sample_size:1000000,index_start:0,index_end_inclusive:999999,previous_analysis_cohort:"wanted_landing_v1-C8",candidate_analysis_cohort:"wanted_landing_v1-C9",previous_source_profile:"0.32-RSDK1",previous_source_sha256:previousDigest,candidate_source_profile:"0.43-RSM1",candidate_source_sha256:candidateDigest,previous_variant_by_candidate_ramp:table,previous_variant_totals:{control:variantTotals[0],proof:variantTotals[1],developer:variantTotals[2]},candidate_ramp_totals:{control:candidateTotals[0],proof:candidateTotals[1]},previous_variant_candidate_ramp_cramers_v_ppm:treatmentPpm,old_new_decile_cramers_v_ppm:decilePpm,bucket_pearson_correlation_ppm:correlationPpm,exact_bucket_match_count:same,modulo_16_difference_residue_counts:residues,observed_modulo_16_difference_residues:observedResidues,diagnostics_sha256:await sha256(diagnostics),gates,cohort_separation_verified:separation,activation_recommendation:"hold",remediation:"Replace the candidate cohort bucket derivation with a domain-separated cryptographic hash, issue a new candidate cohort and source profile, then repeat every rollout audit before activation review."},certificateDigest=await sha256(unsigned);certificate={...unsigned,certificate_sha256:certificateDigest};deterministic=await sha256(unsigned)===certificateDigest}const valid=errors.length===0;return{profile:PROFILE,evaluation_status:valid?"complete":"invalid",readiness:"hold",shape_verified:shape,sample_verified:sample,cohort_identity_verified:identity,source_binding_verified:source,deterministic_certificate_verified:deterministic,cohort_separation_verified:separation,certificate:valid?certificate:null,activation_authorized:false,reads_user_identifiers:false,uses_live_traffic:false,counts_exposures:false,supports_version_selection:false,changes_live_allocation:false,changes_live_phase:false,deploys:false,errors,findings}}
export async function runCli(args=[],io={}){const out=io.stdout||((value)=>console.log(value)),err=io.stderr||((value)=>console.error(value)),read=io.readFile||(async path=>(await import("node:fs/promises")).readFile(path,"utf8")),usage="Usage:\n  node wanted-rollout-cohort-separation.mjs --conformance\n  node wanted-rollout-cohort-separation.mjs --audit BUNDLE.json";try{const bundle=args.length===1&&args[0]==="--conformance"?referenceBundle():args.length===2&&args[0]==="--audit"?JSON.parse(await read(args[1])):null;if(!bundle){err(usage);return 2}const result=await auditCohortSeparation(bundle);out(JSON.stringify(result,null,2));return result.evaluation_status==="complete"?0:1}catch(error){err(String(error?.message||error));return 2}}
const normalized=value=>value.replaceAll("\\","/").replace(/^\/([A-Za-z]:\/)/,"$1").toLowerCase();if(typeof process!=="undefined"&&process.argv?.[1]&&import.meta.url.startsWith("file:")&&normalized(decodeURIComponent(new URL(import.meta.url).pathname))===normalized(process.argv[1]))process.exitCode=await runCli(process.argv.slice(2));`;

export const experimentRolloutCohortSeparationVerifierContract = {
  format: "JavaScript ESM",
  runtime_dependencies: 0,
  exports: ["referenceBundle", "auditCohortSeparation", "CLI_EXIT_CODES", "runCli"],
  cli: {
    runtime: "Node.js 22+",
    modes: {
      conformance: "reproduce the frozen C8-to-C9 hold certificate",
      audit: "review one supplied source-bound cohort-separation bundle",
    },
    exit_codes: EXPERIMENT_ROLLOUT_COHORT_SEPARATION_CLI_EXIT_CODES,
  },
} as const;

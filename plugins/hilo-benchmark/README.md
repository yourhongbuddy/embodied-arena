# HILO Benchmark plugin (preview)

Skills-only evidence auditing for HILO / WANTED-10K. This repository package is not an official directory listing or a robot certification.

Use `skills/hilo-audit/SKILL.md` in a client that supports Agent Skills. For a local Claude Code test, add this repository as a marketplace and select `hilo-benchmark@hilo-tools` after the branch is available. For ChatGPT/Codex, the root `plugin.json` is a portable package candidate; follow current provider validation and review instructions. No installation or account submission was performed by creating these files.

The package has no hooks, executables, secret access or MCP connection. It helps interpret versioned protocol evidence; it does not generate official scores from unsupported data. Provider-specific install tests and an owner-selected license remain prerequisites for general redistribution.

Try: “Audit this robot benchmark claim for evidence provenance and unsupported 10,000-hour conclusions.” Never upload private participant recordings without a separate approved process.
